# Secrets locales de ejemplo (no commitear valores reales de producción)

# Web Push (mismo par VAPID en front public key y Edge private+public)
# Generar: npx web-push generate-vapid-keys
VITE_VAPID_PUBLIC_KEY=
