import type { SwitcherProps } from './switcher.props';
declare const Switcher: ({ size, checked, onChange, SwitcherPropsData, switcherProps, readOnly, disabled, children, ref, ...rest }: SwitcherProps) => JSX.Element;
export default Switcher;
