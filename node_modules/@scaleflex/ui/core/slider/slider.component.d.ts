import type { SliderProps } from './slider.props';
declare const Slider: ({ defaultValue, disabled, disableSwap, hideTrack, hideAnnotation, value: valueProp, min, max, onChange, onMouseDown, onMouseUp, step, labelTooltip, annotation, components, componentsProps, ref, ...rest }: SliderProps) => JSX.Element;
export default Slider;
