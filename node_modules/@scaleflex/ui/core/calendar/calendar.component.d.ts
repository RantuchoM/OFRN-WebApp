import { CalendarProps } from './calendar.props';
declare const Calendar: ({ value, onChange, autoSelectToday, onCancel, onDateClick, enableAutoSelect, maxDate, minDate, calendarStyles, ref, ...rest }: CalendarProps) => JSX.Element;
export default Calendar;
