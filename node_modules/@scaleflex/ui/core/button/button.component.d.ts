import type { ButtonProps } from './button.props';
declare const Button: ({ children, startIcon, endIcon, badge, color, active, buttonType, sideBarType, size, loading, disabled, ref, ...rest }: ButtonProps) => JSX.Element;
export default Button;
