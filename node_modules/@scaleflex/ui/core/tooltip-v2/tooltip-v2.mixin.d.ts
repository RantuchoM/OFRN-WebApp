import { RuleSet } from 'styled-components';
import type { WithTheme } from '../../theme/entity';
export declare const variantMixin: {
    default: ({ theme: { palette } }: WithTheme) => RuleSet;
    warning: ({ theme: { palette } }: WithTheme) => RuleSet;
    info: ({ theme: { palette } }: WithTheme) => RuleSet;
    error: ({ theme: { palette } }: WithTheme) => RuleSet;
};
