import type { TabProps } from './tab.props';
declare const Tab: {
    ({ icon, label, badge, active, disabled, notification, className, size, ref, ...rest }: TabProps): JSX.Element;
    displayName: string;
};
export default Tab;
