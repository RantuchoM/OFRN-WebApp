import type { StatusLabelProps } from './status-label.props';
declare const StatusLabel: ({ label, variant, type, size, iconSize, ref, ...rest }: StatusLabelProps) => JSX.Element;
export default StatusLabel;
