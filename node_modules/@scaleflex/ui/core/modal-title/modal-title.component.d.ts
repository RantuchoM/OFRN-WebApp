import type { ModalTitleProps } from './modal-title.props';
declare const ModalTitle: {
    ({ secondary, onClose, primaryLabelStyles, iconShadow, variant, ref, ...rest }: ModalTitleProps): JSX.Element;
    displayName: string;
};
export default ModalTitle;
