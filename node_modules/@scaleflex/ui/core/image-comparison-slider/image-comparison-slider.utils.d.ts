export declare const getHorizontalPosition: ({ cursorHorizontalPosition, width, left, handleWidth, }: {
    cursorHorizontalPosition: number;
    width: number;
    left: number;
    handleWidth: number;
}) => {
    horizontalPosition?: number;
} | undefined;
