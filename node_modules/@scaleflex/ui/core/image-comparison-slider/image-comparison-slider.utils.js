export var getHorizontalPosition = function getHorizontalPosition(_ref) {
  var cursorHorizontalPosition = _ref.cursorHorizontalPosition,
    width = _ref.width,
    left = _ref.left,
    handleWidth = _ref.handleWidth;
  var isHandleInBoundaries = cursorHorizontalPosition <= width + left - handleWidth / 2 && cursorHorizontalPosition >= left;
  var isHandleOutsideRightBoundary = cursorHorizontalPosition > width + left - handleWidth / 2;
  var isHandleOutsideLeftBoundary = cursorHorizontalPosition < left;
  if (isHandleInBoundaries) {
    //set handle at cursor position
    return {
      horizontalPosition: (cursorHorizontalPosition - left) / width
    };
  }
  if (isHandleOutsideRightBoundary) {
    //set handle at extreme right
    return {
      horizontalPosition: 1
    };
  }
  if (isHandleOutsideLeftBoundary) {
    //set handle at extreme left
    return {
      horizontalPosition: 0
    };
  }
};