import type { InputLocalizationProps } from './input-localization.props';
declare const InputLocalization: ({ children, onChange, value, icon, size, MenuProps, readOnly, disabled, renderLabel, multiple, ref, ...rest }: InputLocalizationProps) => JSX.Element;
export default InputLocalization;
