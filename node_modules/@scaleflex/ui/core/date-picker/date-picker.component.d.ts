import { DatePickerProps } from './date-picker.props';
declare const Datepicker: ({ value, onChange, autoSelectToday, enableAutoSelect, maxDate, minDate, position, label, size, hint, popperOptions, zIndex, InputProps: InputPropsData, calendarStyles, popperWrapperStyles, inputGroupProps, fullWidth, placeholder, ref, ...rest }: DatePickerProps) => JSX.Element;
export default Datepicker;
