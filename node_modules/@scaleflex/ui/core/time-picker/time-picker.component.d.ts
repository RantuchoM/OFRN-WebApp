import { TimePickerProps } from './time-picker.props';
declare const TimePicker: ({ position, size, popperOptions, InputProps: InputPropsData, fullWidth, inputGroupProps, onChange, ref, ...rest }: TimePickerProps) => JSX.Element;
export default TimePicker;
