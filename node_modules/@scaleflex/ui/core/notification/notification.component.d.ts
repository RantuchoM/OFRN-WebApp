import { NotificationProps } from './notification.props';
declare const Notification: ({ status, removeBackground, hideIcon, title, message, onClose, ref, ...rest }: NotificationProps) => JSX.Element;
export default Notification;
